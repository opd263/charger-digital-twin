# Charger Digital Twin (Next.js + Tailwind)

## Setup
1. Install dependencies
   npm install

2. Run dev server
   npm run dev
   Open http://localhost:3000

## Notes
- Mock APIs available under /pages/api/charger/[id]/
- Replace assets in /public/icons and /public/images with Figma exported SVGs/PNGs
- Tailwind tokens set in tailwind.config.js — tune colors/spacing to match Figma exactly
